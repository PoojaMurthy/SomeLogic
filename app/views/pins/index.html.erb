
<div id="pins">

<% @pins.each do |pin| %>

<div class="box">
	
	<%= link_to image_tag pin.image.url %>
	<p class="title"><%= link_to pin.title, pin %></p>

	<p class="user"><%= pin.user.name %> </p>

</div>
<% end %>

</div>
